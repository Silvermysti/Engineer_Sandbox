# Engineer Sandbox — Python dependencies
# Python 3.13+ required (see .python-version)
# Install: pip install -r requirements.txt

# --- Core framework ---
typer>=0.25.0          # CLI commands (start, reset)
rich>=15.0.0           # Terminal UI: panels, progress bars, prompts

# --- HTTP / async ---
httpx>=0.28.1          # Async HTTP client (Groq API calls)
aiohttp>=3.13.5        # Async HTTP (retained for future Ollama streaming)

# --- Data validation ---
pydantic>=2.13.3       # Typed models: ScenarioSpec, AgentSpec, AgentReaction, etc.
python-dotenv>=1.0.0   # Environment variable loading

# --- Standard library (no install needed) ---
# asyncio   — async/await runtime
# sqlite3   — SQLite persistence (storage/database.py)
# json      — JSON parsing
# re        — regex (utils/json_parser.py)
# os        — file system ops
